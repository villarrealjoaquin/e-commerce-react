import ShoppingCartIcon from '@mui/icons-material/ShoppingCart';

const CartWidget = () => {
    return (
        <a href=""> <ShoppingCartIcon  className="cart" color="primary" fontSize="large" /></a>
    )
}

export default CartWidget